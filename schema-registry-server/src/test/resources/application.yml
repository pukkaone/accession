schema-registry:
  branch: main
  repository-uri: ssh://git@example.com/config-repo.git

server:
  port: 8081
  servlet.context-path: /

spring:
  cloud.config.enabled: false
  main.banner-mode: 'off'
